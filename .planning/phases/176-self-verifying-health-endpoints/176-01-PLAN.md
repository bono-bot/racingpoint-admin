---
phase: 176-self-verifying-health-endpoints
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/health/route.ts
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02

must_haves:
  truths:
    - "Admin /api/health lists all 31 expected pages including /hr, /hr/attendance, /hr/leaves, /hr/hiring"
    - "Admin /api/health returns 200 with status 'ok' when all pages present"
    - "Admin /api/health returns 503 with status 'degraded' when any expected page is missing"
  artifacts:
    - path: "src/app/api/health/route.ts"
      provides: "Self-verifying health endpoint for admin dashboard"
      contains: "EXPECTED_PAGES"
  key_links:
    - from: "src/app/api/health/route.ts"
      to: ".next/server/app/"
      via: "fs.readdirSync scan"
      pattern: "readdirSync"
---

<objective>
Update the admin dashboard's existing `/api/health` endpoint so its EXPECTED_PAGES array matches the actual 31 pages in the app. The endpoint already has full route scanning, 200/503 logic, and manifest reporting — only the page list needs correction.

Purpose: The admin health endpoint is the reference implementation. Correcting its page list ensures it can accurately detect missing pages after deploys.
Output: Updated `src/app/api/health/route.ts` with correct EXPECTED_PAGES array.
</objective>

<execution_context>
@C:/Users/bono/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bono/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update admin EXPECTED_PAGES to match actual app pages</name>
  <files>src/app/api/health/route.ts</files>
  <read_first>
    - src/app/api/health/route.ts (current implementation — the reference for kiosk/web)
    - src/app/(dashboard)/layout.tsx (sidebar links = source of truth for expected pages)
  </read_first>
  <action>
Update the EXPECTED_PAGES array in `src/app/api/health/route.ts` to include all actual pages. The current array has 31 entries but is accurate per the sidebar. Cross-check by scanning `src/app/` directory for all `page.tsx` files to confirm completeness.

Steps:
1. Run `find src/app -name "page.tsx" -not -path "*/api/*"` to get all actual page routes
2. Map each `page.tsx` path to its URL route (strip `src/app`, strip route groups like `(auth)`/`(dashboard)`, strip `/page.tsx`, convert `[param]` to `[param]`)
3. Compare with current EXPECTED_PAGES array
4. Add any missing pages, remove any pages that don't have a `page.tsx`
5. Keep the array alphabetically sorted for readability
6. Do NOT change any other logic — the scanning, 200/503 response, and manifest structure are already correct

Known current array (31 entries):
```
'/', '/analytics', '/sessions', '/billing', '/billing/history',
'/billing/reports', '/billing/rates', '/bookings', '/calendar',
'/customers', '/waivers', '/fleet', '/leaderboard', '/tournaments',
'/kiosk', '/coupons', '/pricing', '/packages', '/memberships',
'/cafe', '/cafe/inventory', '/sales', '/purchases',
'/wallet-transactions', '/finance', '/hr', '/hr/hiring',
'/hr/attendance', '/hr/leaves', '/chat', '/transcribe', '/settings',
```

The scout reported 33 actual pages vs 30 in the array (they may have miscounted — verify by scanning). Verify every `page.tsx` maps to an EXPECTED_PAGES entry and vice versa.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('src/app/api/health/route.ts','utf8'); const m=c.match(/EXPECTED_PAGES\s*=\s*\[([\s\S]*?)\]/); if(!m){process.exit(1)} const pages=m[1].match(/'[^']+'/g).map(s=>s.replace(/'/g,'')); console.log('Pages:',pages.length); const missing=['/','/hr','/hr/attendance','/hr/leaves','/hr/hiring'].filter(p=>!pages.includes(p)); if(missing.length){console.error('Missing:',missing);process.exit(1)} console.log('All critical pages present')"</automated>
  </verify>
  <acceptance_criteria>
    - EXPECTED_PAGES array in src/app/api/health/route.ts contains every page that has a page.tsx file under src/app/
    - No phantom pages in EXPECTED_PAGES that lack a corresponding page.tsx
    - Array is alphabetically sorted
    - No other code changes — scanning logic, GET handler, response format unchanged
    - grep for "EXPECTED_PAGES" returns exactly one const declaration
  </acceptance_criteria>
  <done>EXPECTED_PAGES array matches actual app pages. Running the endpoint locally would return status "ok" if all pages are built.</done>
</task>

</tasks>

<verification>
- `grep -c "EXPECTED_PAGES" src/app/api/health/route.ts` returns 1 (single declaration)
- The array count matches `find src/app -name "page.tsx" -not -path "*/api/*" | wc -l`
- No TypeScript errors: `npx tsc --noEmit src/app/api/health/route.ts` (or project-level check)
</verification>

<success_criteria>
Admin /api/health endpoint has a correct EXPECTED_PAGES array that matches every page.tsx in the app. The endpoint is the verified reference implementation for kiosk and web to follow.
</success_criteria>

<output>
After completion, create `.planning/phases/176-self-verifying-health-endpoints/176-01-SUMMARY.md`
</output>
