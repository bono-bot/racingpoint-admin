---
phase: 161-fleet-monitoring
plan: 02
type: execute
wave: 2
depends_on: ["161-01"]
files_modified:
  - src/app/(dashboard)/fleet/page.tsx
autonomous: true
requirements: [FLEET-10]

must_haves:
  truths:
    - "Staff can view a reverse-chronological activity log of pod events"
    - "Staff can filter the activity log by specific pod"
    - "Log shows last 100 events with a Load more button to fetch additional events"
    - "Activity log updates automatically via the same 5-second polling"
  artifacts:
    - path: "src/app/(dashboard)/fleet/page.tsx"
      provides: "Activity log section below pod grid"
      contains: "activity"
      min_lines: 150
  key_links:
    - from: "src/app/(dashboard)/fleet/page.tsx"
      to: "/api/rc/activity"
      via: "useSWR fetching global activity endpoint"
      pattern: "activity"
    - from: "src/app/(dashboard)/fleet/page.tsx"
      to: "fleetApi.getActivity"
      via: "API call with limit and pod filter params"
      pattern: "getActivity"
---

<objective>
Add a global activity log section to the fleet dashboard page, showing recent pod events in a filterable, paginated table.

Purpose: Staff need to see what happened across the fleet — game launches, billing events, maintenance triggers — to understand pod behavior and troubleshoot issues.
Output: Activity log table with pod filter, reverse-chronological ordering, and "Load more" pagination.
</objective>

<execution_context>
@C:/Users/bono/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bono/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/161-fleet-monitoring/161-01-SUMMARY.md

<interfaces>
<!-- RC Activity API endpoints (from routes.rs) -->

GET /api/v1/activity?limit=100
  Returns: Array of ActivityEntry objects (NOT wrapped in { data: [...] })
  ```typescript
  interface ActivityEntry {
    id: string;
    pod_id: string;
    pod_number: number;
    timestamp: string;    // ISO-8601
    category: string;     // e.g., "billing", "game", "maintenance", "system"
    action: string;       // e.g., "session_started", "game_launched", "preflight_failed"
    details: string;      // human-readable description
    source: string;       // e.g., "racecontrol", "rc-agent", "scheduler"
  }
  ```

GET /api/v1/pods/{pod_id}/activity?limit=100
  Returns: Same shape, filtered to specific pod

<!-- From Plan 01 -->
From src/lib/api/fleet.ts:
```typescript
export interface PodFleetStatus { ... }
export interface FleetHealthResponse { pods: PodFleetStatus[], timestamp: string }
export const fleetApi = {
  getHealth: () => rcFetch('/fleet/health'),
  listPods: () => rcFetch('/pods'),
  // ... will add getActivity here
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add activity API functions to fleet module</name>
  <files>src/lib/api/fleet.ts</files>
  <read_first>
    - src/lib/api/fleet.ts
    - src/lib/api/base.ts
  </read_first>
  <action>
In `src/lib/api/fleet.ts`:

1. Add `ActivityEntry` interface with fields: `id: string`, `pod_id: string`, `pod_number: number`, `timestamp: string`, `category: string`, `action: string`, `details: string`, `source: string`. Export the type.

2. Add two functions to `fleetApi`:
   - `getActivity: (limit = 100): Promise<ActivityEntry[]> => rcFetch('/activity?limit=' + limit)` — global activity log
   - `getPodActivity: (podId: string, limit = 100): Promise<ActivityEntry[]> => rcFetch('/pods/' + podId + '/activity?limit=' + limit)` — per-pod activity

Note: The RC API returns a plain JSON array (not wrapped), so the return type is `ActivityEntry[]` directly.
  </action>
  <verify>
    <automated>cd C:/Users/bono/racingpoint/racingpoint-admin && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ActivityEntry" src/lib/api/fleet.ts
    - grep -q "getActivity" src/lib/api/fleet.ts
    - grep -q "getPodActivity" src/lib/api/fleet.ts
  </acceptance_criteria>
  <done>Fleet API module exports ActivityEntry type and getActivity/getPodActivity functions.</done>
</task>

<task type="auto">
  <name>Task 2: Activity log section with pod filter and Load more</name>
  <files>src/app/(dashboard)/fleet/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/fleet/page.tsx (from Plan 01)
    - src/lib/api/fleet.ts (after Task 1 updates)
  </read_first>
  <action>
Add an "Activity Log" section BELOW the pod grid in the fleet page.

**State management:**
- `podFilter: string` state — empty string means "all pods", otherwise holds a pod_id value
- `limit: number` state — starts at 100, incremented by 100 on "Load more"

**Data fetching:**
- Second `useSWR` call with key that includes podFilter and limit: `['/fleet/activity', podFilter, limit]`
- Fetcher: if `podFilter` is set, call `fleetApi.getPodActivity(podFilter, limit)`, else `fleetApi.getActivity(limit)`
- Set `refreshInterval: 5000` to match fleet health polling

**Filter bar:**
- Label "Activity Log" as h2 (text-xl font-semibold mt-8 mb-4)
- Dropdown select (`bg-rp-card border border-rp-border rounded-lg px-3 py-1.5 text-sm text-white`):
  - Option "All Pods" (value="")
  - Options "Pod 1" through "Pod 8" — use the pods data from the health SWR to get pod_id values
  - When changed, set podFilter and reset limit to 100

**Activity table:**
- Full-width table with `bg-rp-card border border-rp-border rounded-lg overflow-hidden`
- Table header row: Time, Pod, Category, Action, Details, Source — with `text-left text-xs uppercase text-rp-grey px-4 py-2 border-b border-rp-border`
- Table body rows with `border-b border-rp-border/50 hover:bg-rp-black/30 transition-colors`:
  - Time: format timestamp to IST locale (HH:MM:SS format, with date if not today)
  - Pod: "Pod {pod_number}"
  - Category: styled badge — use category-specific colors:
    - billing: `bg-blue-900/40 text-blue-400`
    - game: `bg-purple-900/40 text-purple-400`
    - maintenance: `bg-yellow-900/40 text-yellow-400`
    - system: `bg-neutral-800 text-neutral-400`
    - default: `bg-neutral-800 text-neutral-300`
  - Action: text-sm text-white
  - Details: text-sm text-neutral-400, truncate with max-w-xs and title attribute for full text
  - Source: text-xs text-rp-grey

**Load more button:**
- Shown only when activity array length equals current limit (means more might exist)
- Button: `w-full py-2 text-sm text-rp-grey hover:text-white bg-rp-card border border-rp-border rounded-lg mt-2`
- On click: increment limit by 100

**Empty state:** "No activity recorded" centered message when array is empty.
**Loading state:** "Loading activity..." text while data is null.
  </action>
  <verify>
    <automated>cd C:/Users/bono/racingpoint/racingpoint-admin && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Activity Log" src/app/(dashboard)/fleet/page.tsx
    - grep -q "podFilter" src/app/(dashboard)/fleet/page.tsx
    - grep -q "Load more" src/app/(dashboard)/fleet/page.tsx
    - grep -q "getActivity" src/app/(dashboard)/fleet/page.tsx
    - grep -q "category" src/app/(dashboard)/fleet/page.tsx
  </acceptance_criteria>
  <done>Activity log shows reverse-chronological pod events in a table, filterable by pod via dropdown, with "Load more" button for pagination. Auto-refreshes every 5 seconds.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. Activity API functions exist: `grep "getActivity\|getPodActivity" src/lib/api/fleet.ts`
3. Activity log section in page: `grep "Activity Log" src/app/(dashboard)/fleet/page.tsx`
4. Pod filter dropdown: `grep "podFilter" src/app/(dashboard)/fleet/page.tsx`
5. Load more pagination: `grep "Load more" src/app/(dashboard)/fleet/page.tsx`
</verification>

<success_criteria>
- Activity log table appears below pod grid on fleet page
- Table shows timestamp, pod number, category (color-coded), action, details, source
- Dropdown filter lets staff view events for a specific pod or all pods
- "Load more" button loads additional events (100 at a time)
- Activity data auto-refreshes every 5 seconds via SWR
- TypeScript compiles with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/161-fleet-monitoring/161-02-SUMMARY.md`
</output>
