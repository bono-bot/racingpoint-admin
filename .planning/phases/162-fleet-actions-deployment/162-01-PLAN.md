---
phase: 162-fleet-actions-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/api/fleet.ts
  - src/components/ConfirmDialog.tsx
  - src/app/(dashboard)/fleet/page.tsx
autonomous: true
requirements: [FLEET-02, FLEET-03, FLEET-04, FLEET-05, FLEET-06]

must_haves:
  truths:
    - "Staff can wake, shutdown, or restart any individual pod from its card"
    - "Staff can lockdown/unlock and enable/disable any individual pod"
    - "Staff can toggle maintenance mode on a pod card"
    - "Staff can execute bulk actions (wake-all, shutdown-all, restart-all, lockdown-all) from a toolbar"
    - "Destructive actions (shutdown, restart, lockdown) show a confirmation dialog before executing"
    - "Action feedback appears as a toast notification"
  artifacts:
    - path: "src/lib/api/fleet.ts"
      provides: "Pod action API functions"
      exports: ["wakePod", "shutdownPod", "restartPod", "lockdownPod", "unlockPod", "enablePod", "disablePod", "clearMaintenance", "wakeAll", "shutdownAll", "restartAll", "lockdownAll"]
    - path: "src/components/ConfirmDialog.tsx"
      provides: "Reusable confirmation dialog"
      exports: ["ConfirmDialog"]
    - path: "src/app/(dashboard)/fleet/page.tsx"
      provides: "Fleet page with action buttons and bulk bar"
      contains: "BulkActionBar"
  key_links:
    - from: "src/app/(dashboard)/fleet/page.tsx"
      to: "src/lib/api/fleet.ts"
      via: "fleetApi.wakePod(), fleetApi.shutdownPod(), etc."
      pattern: "fleetApi\\.(wake|shutdown|restart|lockdown|unlock|enable|disable|clearMaintenance)"
    - from: "src/app/(dashboard)/fleet/page.tsx"
      to: "src/components/ConfirmDialog.tsx"
      via: "import ConfirmDialog"
      pattern: "import.*ConfirmDialog"
---

<objective>
Add individual pod actions, maintenance mode toggle, bulk fleet actions, and a confirmation dialog to the fleet dashboard.

Purpose: Staff can control pods directly from the fleet dashboard without accessing RaceControl CLI.
Output: Fleet page with working action buttons on pod cards, bulk action toolbar, and confirm dialog for destructive actions.
</objective>

<execution_context>
@C:/Users/bono/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bono/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/(dashboard)/fleet/page.tsx
@src/lib/api/fleet.ts
@src/lib/api/base.ts
@src/hooks/useAuth.ts

<interfaces>
<!-- Existing fleet API module -->
From src/lib/api/fleet.ts:
```typescript
import { rcFetch } from './base';
export const fleetApi = {
  listPods: () => rcFetch('/pods'),
  getHealth: (): Promise<FleetHealthResponse> => rcFetch('/fleet/health'),
  getActivity: (limit = 100): Promise<ActivityEntry[]> => rcFetch('/activity?limit=' + limit),
  getPodActivity: (podId: string, limit = 100): Promise<ActivityEntry[]> =>
    rcFetch('/pods/' + podId + '/activity?limit=' + limit),
};
```

From src/lib/api/base.ts:
```typescript
export async function rcFetch(path: string, options: RequestInit = {}): Promise<any>;
```

From src/hooks/useAuth.ts:
```typescript
export function useAuth(): AuthContextValue;
// AuthContextValue: { user, isAuthenticated, isAdmin, logout }
```

From src/components/Toast.tsx (sonner shim):
```typescript
// Usage: toast.success('message') or toast.error('message')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fleet action API functions and ConfirmDialog component</name>
  <files>src/lib/api/fleet.ts, src/components/ConfirmDialog.tsx</files>
  <read_first>src/lib/api/fleet.ts, src/lib/api/base.ts</read_first>
  <action>
1. In `src/lib/api/fleet.ts`, add these functions to the `fleetApi` object:
   - `wakePod: (podId: string) => rcFetch('/pods/' + podId + '/wake', { method: 'POST' })`
   - `shutdownPod: (podId: string) => rcFetch('/pods/' + podId + '/shutdown', { method: 'POST' })`
   - `restartPod: (podId: string) => rcFetch('/pods/' + podId + '/restart', { method: 'POST' })`
   - `lockdownPod: (podId: string) => rcFetch('/pods/' + podId + '/lockdown', { method: 'POST' })`
   - `unlockPod: (podId: string) => rcFetch('/pods/' + podId + '/lockdown', { method: 'DELETE' })`
   - `enablePod: (podId: string) => rcFetch('/pods/' + podId + '/enable', { method: 'POST' })`
   - `disablePod: (podId: string) => rcFetch('/pods/' + podId + '/disable', { method: 'POST' })`
   - `clearMaintenance: (podId: string) => rcFetch('/pods/' + podId + '/clear-maintenance', { method: 'POST' })`
   - `wakeAll: () => rcFetch('/pods/wake-all', { method: 'POST' })`
   - `shutdownAll: () => rcFetch('/pods/shutdown-all', { method: 'POST' })`
   - `restartAll: () => rcFetch('/pods/restart-all', { method: 'POST' })`
   - `lockdownAll: () => rcFetch('/pods/lockdown-all', { method: 'POST' })`

2. Create `src/components/ConfirmDialog.tsx` — a reusable modal:
   - Props: `{ open: boolean; title: string; message: string; confirmLabel?: string; variant?: 'danger' | 'warning'; onConfirm: () => void; onCancel: () => void }`
   - Renders a centered overlay (bg-black/60 backdrop) with a card (bg-rp-card border border-rp-border rounded-lg p-6)
   - Title in white, message in text-neutral-400
   - Two buttons: Cancel (bg-neutral-700 hover:bg-neutral-600) and Confirm (bg-red-600 hover:bg-red-700 for danger, bg-yellow-600 for warning)
   - confirmLabel defaults to "Confirm"
   - When `open` is false, render null
   - Export as named export `ConfirmDialog`
  </action>
  <acceptance_criteria>
    - grep -q "wakePod" src/lib/api/fleet.ts
    - grep -q "shutdownAll" src/lib/api/fleet.ts
    - grep -q "clearMaintenance" src/lib/api/fleet.ts
    - grep -q "export.*ConfirmDialog" src/components/ConfirmDialog.tsx
    - grep -q "onConfirm" src/components/ConfirmDialog.tsx
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/bono/racingpoint/racingpoint-admin && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Fleet API has 12 new action functions. ConfirmDialog component exists with open/title/message/onConfirm/onCancel props.</done>
</task>

<task type="auto">
  <name>Task 2: Add action buttons to pod cards and bulk action bar to fleet page</name>
  <files>src/app/(dashboard)/fleet/page.tsx</files>
  <read_first>src/app/(dashboard)/fleet/page.tsx, src/components/ConfirmDialog.tsx, src/lib/api/fleet.ts</read_first>
  <action>
Modify `src/app/(dashboard)/fleet/page.tsx`:

1. **Add imports**: `ConfirmDialog` from '@/components/ConfirmDialog', `useAuth` from '@/hooks/useAuth', `toast` from 'sonner'.

2. **Add confirm state** in FleetPage: `const [confirm, setConfirm] = useState<{ title: string; message: string; action: () => Promise<void> } | null>(null)`.

3. **Create helper** `execAction(label: string, fn: () => Promise<void>)` that:
   - Calls fn(), then `toast.success(label + ' completed')`, then `mutate()` to refresh SWR
   - On error: `toast.error(label + ' failed: ' + (e as Error).message)`

4. **Create helper** `confirmAction(title: string, message: string, fn: () => Promise<void>)` that sets the confirm state.

5. **Modify PodCard** to accept `onAction: (label: string, fn: () => Promise<void>, needsConfirm: boolean) => void` and `isAdmin: boolean` props. Add a button row below the connection info:
   - Row 1: Wake (green text), Shutdown (red, confirm), Restart (yellow, confirm) — three small buttons
   - Row 2: Lockdown (red, confirm) / Unlock (green), Enable (green) / Disable (red) — conditional on pod state (use `in_maintenance` for lockdown state context)
   - Row 3: Maintenance toggle — a small switch/button: if `pod.in_maintenance`, show "Clear Maintenance" (green); else show "Set Maintenance" (yellow via lockdown)
   - Button style: `text-xs px-2 py-1 rounded` with color variants per action
   - Use `pod.pod_id` for API calls. If pod_id is null, disable all action buttons.

6. **Add BulkActionBar** component above the pod grid:
   - A flex row with: "Fleet Actions" label, then buttons: Wake All (emerald), Shutdown All (red, confirm), Restart All (yellow, confirm), Lockdown All (red, confirm)
   - Same button styling as pod cards but slightly larger (text-sm px-3 py-1.5)
   - Separated from pod grid by a bottom border

7. **Render ConfirmDialog** at the bottom of the page, controlled by `confirm` state. On confirm: call `confirm.action()` then clear state. On cancel: clear state.

8. **Pass `isAdmin`** from `useAuth()` — not used in this plan but threaded through for Plan 02.
  </action>
  <acceptance_criteria>
    - grep -q "BulkActionBar" src/app/(dashboard)/fleet/page.tsx
    - grep -q "ConfirmDialog" src/app/(dashboard)/fleet/page.tsx
    - grep -q "wakePod\|shutdownPod\|restartPod" src/app/(dashboard)/fleet/page.tsx
    - grep -q "wakeAll\|shutdownAll" src/app/(dashboard)/fleet/page.tsx
    - grep -q "clearMaintenance\|in_maintenance" src/app/(dashboard)/fleet/page.tsx
    - grep -q "toast" src/app/(dashboard)/fleet/page.tsx
    - grep -q "useAuth" src/app/(dashboard)/fleet/page.tsx
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/bono/racingpoint/racingpoint-admin && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Pod cards show action buttons (wake, shutdown, restart, lockdown/unlock, enable/disable, maintenance toggle). Bulk action bar above grid has Wake All, Shutdown All, Restart All, Lockdown All. Destructive actions show confirmation dialog. Toast feedback on success/error. SWR mutate refreshes pod state after actions.</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit`
- Fleet page loads without errors: `npm run build` passes
- ConfirmDialog component exists and exports correctly
- Fleet API has all 12 action functions
</verification>

<success_criteria>
- Pod cards display action buttons for wake, shutdown, restart, lockdown/unlock, enable/disable, and maintenance toggle
- Bulk action bar renders above the pod grid with fleet-wide action buttons
- Destructive actions trigger a confirmation dialog before executing
- Successful actions show a toast and refresh the fleet data
- Failed actions show an error toast
</success_criteria>

<output>
After completion, create `.planning/phases/162-fleet-actions-deployment/162-01-SUMMARY.md`
</output>
